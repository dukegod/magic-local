/**
 * static serve
 */
const http = require('http')
const path = require('path')

class StaticServe{
  constructor(props) {
    super(props)
    this.port = prot;
    this.root = root;
  }
  serve(){
    http.createServer()
  }

}
